---
description: 'Workspace-specific instructions generated by agent-context'
applyTo: '**'
---

# Workspace Context

{% if languages %}
**Stack**: {{ languages | join(", ") }}{% if frameworks %} ({{ frameworks | join(", ") }}){% endif %}

{% endif %}
{% if test_command %}
**Verify changes**: `{{ test_command }}`
{% endif %}
{% if lint_command %}
**Lint**: `{{ lint_command }}`
{% endif %}
{% if build_command %}
**Build**: `{{ build_command }}`
{% endif %}

## Completion Checklist
Before declaring your work done, verify ALL of these:
1. **100% of the task is implemented** — partial work will be rejected
2. **No placeholder content** — search for `TODO`, `FIXME`, `[INSERT`, `PLACEHOLDER` and replace them all
3. **No references to non-existent files** — if you mention a path in docs/code, verify it exists
{% if test_command %}
4. **Tests pass**: run `{{ test_command }}` and confirm green
{% endif %}
{% if lint_command %}
5. **Lint passes**: run `{{ lint_command }}` and fix all errors
{% endif %}
6. **Changes are committed and pushed** — run `git status` to confirm clean tree
7. **PR is created** — run `gh pr list --head $(git branch --show-current)` to confirm

{% if task.get("feedback") %}
## Previous Review Feedback (FIX THESE)
{{ task.feedback }}
{% endif %}

{% if task.get("acceptance_criteria") %}
## Acceptance Criteria
{{ task.acceptance_criteria }}
{% endif %}
